import React from 'react';
import { StyleReset } from 'atomize';
import App from '../../App';
import { Router } from 'react-router-dom';

const Main = () => {
  return (
    <Router>
      <StyleReset />
    </Router>
  );
}

export default Main;